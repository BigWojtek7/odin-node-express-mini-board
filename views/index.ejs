<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <div class="content">
      <div class="messages">
        <% messages.forEach(ms => {%>
          <div class="message">
            <p><%=ms.user%></p>
            <p><%=ms.text%></p>
            <p><%=ms.added.toLocaleString()%></p>
          </div>
          <%});%>
      </div>
      <a href="/new">Add new message</a>
    </div>

  </body>
</html>
